#include <bits/stdc++.h>
using namespace std;

struct node
{
public:
    int data;
    node *next;
    node(int val)
    {
        next = nullptr;
        data = val;
    }
};

int main()
{
    node *head = NULL;

    
}
